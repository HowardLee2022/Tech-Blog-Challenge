<h1>Homepage</h1>
<h1>{{user.email}}</h1>
{{#each userdate}}
    <section class="chirp-box">
        <section><p>created at {{createdAt}}</p></section>
    <a href="/view/{{this.id}}"> <h1>{{title}}</h1> </a>
    <p> {{description}}</p>
    <h3>Posted by:{{User.email}}</h3>
</section>
{{/each}}